version: 2
jobs:
  build:
    docker:
      - image: google/dart:2.2
    steps:
      - checkout
      - run: 
        cd example
        pub run build_runner build
workflows:
  version: 2
  build_and_test:
    jobs:
      - build